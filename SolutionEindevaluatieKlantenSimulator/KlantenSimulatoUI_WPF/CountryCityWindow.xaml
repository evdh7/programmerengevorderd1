<Window x:Class="KlantenSimulatorUI_WPF.CountryCityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KlantenSimulatorUI_WPF"
        mc:Ignorable="d"
        Title="CountryCityWindow" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="4" Margin="5" VerticalAlignment="Center" TextAlignment="Center" Text="Simulation Tool Parameters"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Margin="5" Text="Country"/>
            <ComboBox Grid.Row="1" Grid.Column="1" Margin="5" Name="ComboboxCountries" ></ComboBox>

            <TextBlock Grid.Row="2" Grid.Column="0" Margin="5" Text="City (optional)"/>
            <ListBox Grid.Row="2" Grid.Column="1" Margin="5,10,5,0" Name="ListBoxAllCities" SelectionMode="Extended"/>
            <ListBox Grid.Row="2" Grid.Column="2" Margin="5" Name="ListBoxSelectedCities" SelectionMode="Extended"/>

            <StackPanel Grid.Row="2" Grid.Column="2" VerticalAlignment="Center">
                <Button Content="&gt;&gt;" Margin="5" Click="ButtonAddAll_Click"/>
                <Button Content="&gt;" Margin="5" Click="ButtonAddSelected_Click"/>
                <Button Content="&lt;" Margin="5" Click="ButtonRemoveSelected_Click"/>
                <Button Content="&lt;&lt;" Margin="5" Click="ButtonRemoveAll_Click"/>
            </StackPanel>

            <Button Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="3" Content="Next" Margin="5" Click="ButtonClick_NextWindow"/>



        </Grid>

    </Grid>
</Window>
